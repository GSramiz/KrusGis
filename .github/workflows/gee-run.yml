name: Run Sentinel-2 Script

on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  run-script:
    runs-on: ubuntu-latest

    steps:
    - name: 🔽 Checkout репозитория
      uses: actions/checkout@v3

    - name: 🐍 Установка Python
      uses: actions/setup-python@v5
      with:
        python-version: "3.10"

    - name: 📦 Установка зависимостей
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: 🔐 Декодирование service-account.json
      env:
        GCP_SERVICE_ACCOUNT_B64: ${{ secrets.GCP_SA_KEY }}
      run: |
        echo "$GCP_SERVICE_ACCOUNT_B64" | base64 -d > service-account.json

    - name: ▶️ Запуск скрипта
      env:
        GOOGLE_APPLICATION_CREDENTIALS: service-account.json
        GEE_CREDENTIALS: ${{ secrets.GCP_SA_KEY }}
      run: python main.py
